# Normalize line endings across OSes (kills the LF/CRLF warnings on every commit).
* text=auto eol=lf

# Treat binary assets as binary (no EOL/diff munging).
*.glb   binary
*.hdr   binary
*.wav   binary
*.ogg   binary
*.png   binary
*.jpg   binary
*.jpeg  binary
